{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["videoPath", "targetLanguage"],
  "properties": {
    "videoPath": {
      "type": "string",
      "description": "Absolute path to the local video file selected by the user."
    },
    "sourceLanguage": {
      "type": "string",
      "description": "Source language code or name. Omit or use auto for language detection."
    },
    "targetLanguage": {
      "type": "string",
      "description": "Target language name or code for translated subtitles."
    },
    "subtitleFormat": {
      "type": "string",
      "enum": ["srt", "vtt", "ass"],
      "default": "srt"
    },
    "translate": {
      "type": "boolean",
      "default": true
    }
  },
  "additionalProperties": false
}
