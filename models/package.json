{
  "name": "@slab/models",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "pack": "bun ../scripts/models/pack-models.ts",
    "lint": "oxlint ../scripts/models",
    "lint:fix": "oxlint --fix ../scripts/models"
  },
  "engines": {
    "node": ">=24",
    "bun": ">=1.3.11"
  },
  "devDependencies": {
  },
  "license": "AGPL-3.0-only"
}
