org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
# KGP stays for now (matches the Flutter 3.47.1 template): no stable AGP ships
# built-in Kotlin >= Flutter's 2.2.20 minimum. Revisit when Flutter flips the default.
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
