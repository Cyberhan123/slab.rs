<relevant_memories>
The following rollout summaries from this project's memory store ({{ base_path }}) were selected as relevant to the current request. They are point-in-time records of past sessions, not live state — entries marked STALE may no longer match the codebase, and file or line references inside them may have drifted.

{{ body }}

When these memories inform your answer, cite them in the `<oai-mem-citation>` block as `rollout_summaries/<file>.md:<lines>`.
</relevant_memories>
