<environment_context>
<cwd>{{ cwd if cwd else "(no workspace configured — file and shell tools run relative to the host process working directory, unrestricted by a workspace root)" }}</cwd>
<shell>{{ shell }}</shell>
<os>{{ os }}</os>
<time>{{ timestamp }}</time>
</environment_context>
