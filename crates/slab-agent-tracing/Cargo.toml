[package]
name = "slab-agent-tracing"
version.workspace = true
edition.workspace = true
description = "Session-scoped agent trace logging for slab.rs"
repository.workspace = true
keywords.workspace = true
categories.workspace = true
license.workspace = true

[dependencies]
chrono     = { workspace = true }
serde      = { workspace = true }
serde_json = { workspace = true }
slab-utils = { workspace = true }
tracing    = { workspace = true }
# Payload ids under payloads/ are uuid-v4 based so they are unique across
# writers, turns, and processes (see writer::TraceWriter::write_json_payload).
uuid       = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tracing-subscriber = { workspace = true }
